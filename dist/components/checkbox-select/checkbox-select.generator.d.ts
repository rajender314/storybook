import React from 'react';
import { PiSelectProps } from './checkbox-select';
/**
 *
 * @param props as  PiSelectProps
 * @returns Template based on Library
 * if no library passed default HTML is returned
 */
export declare function generateSelectTemplate({ libraryType, ...props }: PiSelectProps): React.JSX.Element;
